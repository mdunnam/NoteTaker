import SearchClient from "@/components/search/SearchClient";

/**
 * Search & Ask page — wraps the full client-side search and ask experience.
 */
export default function SearchPage() {
  return <SearchClient />;
}
